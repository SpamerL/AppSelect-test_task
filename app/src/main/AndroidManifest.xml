<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.kryak.appselect_test_task">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_custom"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_custom_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppSelecttest_task"
        tools:targetApi="31">
        <activity
            android:name=".presenter.SplashScreen"
            android:exported="true"
            android:theme="@style/Theme.App.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presenter.mainList.MainActivity"
            android:exported="true">
        </activity>
    </application>

</manifest>